#include <string>
#include <vector>

using namespace std;

vector<int> solution(long long n) {
    vector<int> answer;
	long long a =n;
    long long b;
    while (a != 0) {
		b = a % 10;
		answer.push_back(b);
		a = a / 10;
	}
    return answer;
}
